# Local secrets for DnDGem experiments (never commit real values)
# Consumer-owned OpenAI key (BYOK). DnDGem does not ship or proxy keys.
OPENAI_API_KEY=

# Optional experiment model override (default: gpt-5.6-luna)
DNDGEM_OPENAI_MODEL=gpt-5.6-luna
