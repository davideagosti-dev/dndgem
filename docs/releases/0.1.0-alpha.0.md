# DnDGem 0.1.0-alpha.0 — Public Alpha release notes

**Status:** Versioned for Public Alpha publication under dist-tag `alpha` (DND-2.5 Stage B). Registry live status is confirmed only after successful npm publish verification.

**Brand:** DnDGem by FinGem-AI  
**Category:** Content-Aware Adaptive Layout Engine  
**Tagline:** Drag. Resize. Adapt. Without breaking your content.

```text
GEOMETRICALLY FITS  ≠  CONTENT REMAINS USEFUL
```

This is an **early public developer release** (`0.x` prerelease). The Alpha API is intentional but **not** SemVer 1.0. Breaking changes remain possible during 0.x and will be changelogged.

---

## What this Alpha contains

| Package         | Role                                                                |
| --------------- | ------------------------------------------------------------------- |
| `@dndgem/core`  | Domain, constraints, validity, scoring, deterministic `solveLayout` |
| `@dndgem/dom`   | Measurement, resize, pointer drag, Vanilla `createLayoutSession`    |
| `@dndgem/react` | Thin React adapter (`DnDGemProvider` + hooks)                       |

Core capabilities:

- Content-aware constraints (hard geometry vs usefulness)
- VALID / DEGRADED / INVALID evaluation with scoring
- Deterministic adaptive solve / reflow
- Pointer drag → `LayoutIntent` proposals (Core remains authoritative)
- Responsive resize observation
- React and Vanilla integration paths

---

## Install

```bash
# React apps (pulls @dndgem/dom + @dndgem/core)
npm install @dndgem/react@alpha

# Vanilla / DOM apps
npm install @dndgem/dom@alpha

# Core-only (headless solve / evaluate)
npm install @dndgem/core@alpha
```

Published version (once registry-verified): **`0.1.0-alpha.0`**. Dist-tag: **`alpha`** (not `latest`).

---

## Where to start

1. [Quick Start](../guides/quick-start.md) (~10–15 minutes)
2. [Core Concepts](../guides/core-concepts.md)
3. Examples in this repository: `examples/react`, `examples/vanilla` (source is private while the GitHub repo remains private)
4. Public playground: https://dndgem-playground.pages.dev/

Contract: [Alpha API Contract](../architecture/alpha-api-contract.md)

---

## React / Vanilla entrypoints

- **React:** `DnDGemProvider`, `useDnDGem`, `useDnDGemContainer`, `useDnDGemItem` from `@dndgem/react`
- **Vanilla:** `createLayoutSession` (and related DOM helpers) from `@dndgem/dom`
- **Core:** `solveLayout`, `evaluateLayout`, domain factories from `@dndgem/core`

Architecture invariant: `core ← dom ← react`.

---

## Browser support

| Engine / surface              | Classification          |
| ----------------------------- | ----------------------- |
| Chromium (desktop Playwright) | **SUPPORTED FOR ALPHA** |
| Firefox (desktop Playwright)  | **SUPPORTED FOR ALPHA** |
| WebKit (desktop Playwright)   | **SUPPORTED FOR ALPHA** |
| Mobile / touch                | **NOT VALIDATED**       |

Evidence: DND-2.4 matrix — 12 unique e2e specs × 3 engines = 36 executions. Details: [Browser Support](../guides/browser-support.md).

---

## Accessibility baseline

| Capability                       | Status                            |
| -------------------------------- | --------------------------------- |
| Pointer drag                     | Supported                         |
| Escape cancellation              | Supported                         |
| Focus preservation (baseline)    | Supported                         |
| Consumer ARIA semantics          | Preserved                         |
| Keyboard drag                    | **DEFERRED**                      |
| Screen-reader drag announcements | **DEFERRED**                      |
| DOM order vs visual order        | Limitation (absolute positioning) |
| Full WCAG conformance            | **Not claimed**                   |

Details: [Accessibility](../guides/accessibility.md).

---

## Performance context

Historical Technical MVP Core `solveLayout` medians on documented hardware were approximately **0.038–0.279 ms** for dashboard-scale scenes (up to ~40 items). These are **measurements under stated context**, not an SLA. See [Performance](../guides/performance.md).

---

## Known limitations (prominent)

- No Auto-Layout yet (Phase 3)
- No AI (Phase 4, after Auto-Layout)
- No Flutter / Vue / Angular / Svelte adapters
- Keyboard drag deferred
- Screen-reader drag announcements deferred
- Mobile / touch not validated
- Full SSR / hydration not validated (`DnDGemProvider` is client-mount only; module import is safe without `window`)
- Large-N behavior not established
- Absolute-positioning rendering model
- GitHub repository remains **PRIVATE** for this first npm Alpha (source/Issues links in package metadata may be inaccessible to outsiders)

Full list: [Limitations](../guides/limitations.md).

---

## Explicitly NOT implemented

Auto-Layout · AI · Flutter · other JS framework adapters · animation system · persistence helpers · nested layouts · commercial billing · cloud backend · telemetry · custom native DnD engine.

---

## API stability expectations

- Public Alpha freezes the **intentional** export surface documented in the Alpha API Contract.
- This is **not** a SemVer 1.0 stability promise.
- Breaking changes in 0.x are allowed; they will be changelogged.
- Prefer depending on `@alpha` (or an exact `0.1.0-alpha.x`) rather than assuming `latest`.

---

## Feedback

| Channel                            | Path                                               |
| ---------------------------------- | -------------------------------------------------- |
| Product / Alpha developer feedback | **`support@fingem-ai.com`**                        |
| Security reports                   | `security@fingem.ai` (see `SECURITY.md`)           |
| GitHub Issues                      | Not reachable to external users while repo PRIVATE |

Do not treat private GitHub Issues as the primary external feedback path.

---

## Playground / demo

Public playground (no authentication):

**https://dndgem-playground.pages.dev/**

Demonstrates content-aware constraints, VALID / DEGRADED status, resize reflow, and pointer drag. The hosted artifact corresponds to the release codebase (workspace-built static demo; it does not need to install packages from npm).

Local build remains available via `pnpm --filter @dndgem/playground build` → `apps/playground/dist`.

---

## License

MIT — see package `LICENSE` files / repository `LICENSE`.
